# ============================================================
# 仓库根 .gitignore
# Unity 工程自己的忽略规则在 "Do not leave me/.gitignore"，
# 那份管 Library/Temp/Logs/csproj/Wwise 生成物等，**不要在这里重复**。
# 这份只管仓库根这一层和跨目录的通用噪音。
# ============================================================

# 本地高清美术源，与运行时资产重复，不能撑大仓库
/Art/BackUp/

# 待删除暂存区（整理时用来存放确认前的空目录/废弃文件）
/_to_delete/

# ---- 操作系统噪音 ----
.DS_Store
._*
Thumbs.db
ehthumbs.db
desktop.ini

# ---- 编辑器 / IDE ----
.vs/
.vscode/
.idea/
*.swp
# 注意：这里**不能**写 `*~`。Unity 用结尾的 ~ 表示「不导入这个目录」
# （例如 Packages/com.arongranberg.astar/ExampleScenes~/），`*~` 会把
# 这类目录连同里面几千个已入库的文件一起吞掉。

# ---- AI 助手与工具的本地缓存 ----
.claude/
.cursor/
.aider*

# ---- Python（openspec/changes/*/tools/ 下有 .py 脚本）----
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# ---- Node（openspec CLI 若在本地安装）----
node_modules/
npm-debug.log*
